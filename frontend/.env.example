# Public backend API URL bundled into the browser app.
VITE_API_BASE_URL=https://your-backend.onrender.com

# Optional local development proxy target. Leave empty for production builds.
VITE_DEV_API_PROXY_TARGET=http://localhost:3000
